# Evidence register

Source for the newest recorded findings: the owner's supplied
`kaggriculture_service_value_results.zip`. Its 39 manifest-listed file hashes were
checked against the supplied bytes. The screens are development evidence.

| Family | Observations | Task rows | Changed observations | Reported passing tests | Largest candidate callback |
|---|---:|---:|---:|---:|---:|
| Collection completion | 957 | 19,393 | 355 | 64 | 19.06 ms |
| Maintenance deadlines | 957 | 16,571 | 2 | 64 | 20.42 ms |

Both report `REVIEW_REQUIRED_BEFORE_GAMES`. No endpoint experiment for these families
is present in that bundle. The two maintenance changes are mirrored seat views of
the same previously studied seed and step, not independent demonstrations.

Both saved notebooks have 9 of 10 code cells marked executed and no saved error
outputs. A plotting cell lacks an execution count in each. Their completed screen
receipts do not establish clean end-to-end notebook execution. The separate overview
rebuilds reporting from completed aggregate evidence; it does not fabricate missing
research outputs or rerun the screens.

## Outcome follow-up

Rounds 21 and 22 preserve each screened intervention and add 12 outcome-blind
relative descriptors for interpretation. Each has one separately evaluated paired
block, ten figures, and explicit tests. The descriptors do not add another treatment.
No outcome has been assumed for an unexecuted notebook. Review the saved per-round
receipts when available.

## Measurement boundaries

In-game banked money, own-minus-opponent margin, local match result, and Kaggle rating
are distinct measures. There is no verified competition rating in the supplied evidence.
Observation counts do not equal independent experiment counts. Shared controls make
between-family comparisons correlated. The complete callback's workforce restriction
is not resolved by successful variable-length feature extraction.
